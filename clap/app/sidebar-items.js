initSidebarItems({"enum":[["AppSettings","Application level settings, which affect how `App` operates**NOTE:** When these settings are used, they apply only to current command, and are *not* propagated down or up through child or parent subcommands"]],"struct":[["App","Used to create a representation of a command line program and all possible command line arguments. Application settings are set using the \"builder pattern\" with the `.get_matches()` family of methods being the terminal methods that starts the runtime-parsing process. These methods then return information about the user supplied arguments (or lack there of).**NOTE:** There aren't any mandatory \"options\" that one must set. The \"options\" may also appear in any order (so long as one of the `App::get_matches*` methods is the last method called).Examples"]]});